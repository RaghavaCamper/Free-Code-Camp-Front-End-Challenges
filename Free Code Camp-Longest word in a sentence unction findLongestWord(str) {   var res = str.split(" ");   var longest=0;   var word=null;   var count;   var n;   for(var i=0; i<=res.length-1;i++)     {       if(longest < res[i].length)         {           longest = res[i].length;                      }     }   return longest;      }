function findLongestWord(str) {
  var res = str.split(" ");
  var longest=0;
  var word=null;
  var count;
  var n;
  for(var i=0; i<=res.length-1;i++)
    {
      if(longest < res[i].length)
        {
          longest = res[i].length;
          
          }
    }
  return longest;
  
 
}
