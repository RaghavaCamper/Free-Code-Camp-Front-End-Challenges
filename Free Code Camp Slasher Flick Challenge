function slasher(arr, howMany) {
  // it doesn't always pay to be first
  
  var len = arr.length;
   if(howMany > len)
     {
       arr = [];
     }
  else
    {
      for(var i=0;i<howMany;i++)
        {
          arr.shift();
        }
    }
return arr;
}
